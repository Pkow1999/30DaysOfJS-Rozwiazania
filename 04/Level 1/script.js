//3
    let a = 4
    let b = 3
    if(a > b){
        console.log(`${a} is greater than ${b}`)
    }else{
        console.log(`${a} is less than ${b}`)
    }
    console.log(a > b ? `${a} is greater than ${b}` : `${a} is less than ${b}`)

//4
    let nr = 9
    if(nr%2 === 0){
        console.log(`${nr} is an even number`)
    }else{
        console.log(`${nr} is an odd number`)
    }

